apiVersion: v1
kind: Service
metadata:
  name: kk-payments
  labels:
    app: kk-payments
spec:
  selector:
    app: kk-payments
  ports:
  - port: 3001
    targetPort: 80
    protocol: TCP
  type: ClusterIP
