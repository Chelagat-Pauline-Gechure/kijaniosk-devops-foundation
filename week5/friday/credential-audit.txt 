# Credential audit — kijanikiosk-payments pipeline

## Check 1: No literal credentials in Jenkinsfile
Command: grep -iE 'password|secret|token|_key' Jenkinsfile | grep -v 'credentialsId\|withCredentials\|usernameVariable\|passwordVariable\|NEXUS_USER\|NEXUS_PASS'
Result: PASS — all credentials referenced by ID only via withCredentials block. No literal values present.

## Check 2: No credentials in git commit history
Command: git log --all -p | grep -iE 'password=|secret=|_token=|api_key='
Result: PASS — no credential values found in any commit diff.

## Check 3: .npmrc not tracked by git
Command: git ls-files .npmrc kijanikiosk-payments/.npmrc
Result: PASS — .npmrc is listed in .gitignore and not tracked.

## Check 4: No credentials in build log
Command: (manual check of Jenkins Console Output for build #N)
Result: PASS — Nexus password is masked as **** in build log. Only credentialsId 'nexus-credentials' appears in Jenkinsfile source.

## Check 5: No credentials in environment variable declarations
Command: grep -E 'environment|env\.' Jenkinsfile | grep -iE 'password|secret|token'
Result: PASS — environment block contains only APP_NAME, NEXUS_URL, NEXUS_REPO, GIT_SHA, APP_VERSION. No credential values.

---
Summary: All five checks pass. No credentials appear in the Jenkinsfile,
git history, build logs, or environment declarations. Credentials are
stored exclusively in Jenkins credential store and accessed at runtime
via the withCredentials block using the ID 'nexus-credentials'.